#ifndef GLOBALS_H
#define GLOBALS_H

#include <QString>

//State enum
enum STATE{LOADING, MENU, ABOUT, GAME, CLOSE, TESTING};

const int ScreenHeight = 600;
const int ScreenWidth = 800;

#endif // GLOBALS_H
